import { NumberRangePipe } from './number-range.pipe';

describe('NumberRangePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberRangePipe();
    expect(pipe).toBeTruthy();
  });
});
